
function LoginHeader() {
   return (
      <div className="w-full bg-black h-10 text-fuchsia-400 flex items-center justify-center">
         A small project to experiment with JWT authentication between a React frontend and a Spring Boot backend — testing login flow and secure access to a “hello” endpoint.
      </div>
   )
}

export default LoginHeader